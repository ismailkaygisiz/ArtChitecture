import 'package:flutter/material.dart';

class ArtChitectureCustomComponent extends StatelessWidget {
  /// This is a custom component for ArtChitecture
  const ArtChitectureCustomComponent();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
